# Shipit 实施步骤 06 - 安全与权限

## 目标
- 落地凭证安全、路径白名单与越权防护。

## 凭证
- 使用 `.env` 注入；优先 RAM/STS 临时凭证。
- 严禁在日志与配置中输出密钥值。

## 路径与权限
- 上传与发布目录做白名单/范围校验，避免误删或越权写入。

## 兼容建议
- Windows 11 与主流 Linux；默认 Shell 自动选择，可配置覆盖。

